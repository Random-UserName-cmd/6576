rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Settings collection for site notice configuration
    match /settings/{docId} {
      allow read: if true;
      allow write: if true;
    }
  }
}
